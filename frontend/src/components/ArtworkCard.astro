
<style>
    a{
        text-decoration: none;
        color: black;
    }
</style>

<section>
    <h2>Add a New Artwork</h2>
    <form id="addArtworkForm" enctype="multipart/form-data">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required />

        <label for="description">Description:</label>
        <textarea id="description" name="description"></textarea>

        <label for="imageFile">Image:</label>
        <input type="file" id="imageFile" name="imageFile" accept="image/*" required />

        <button type="submit">
            <a href="/images/sunrise.png" target="_blank" class="button">Add Network
        </button>
    </form>
    <p id="addArtworkError" style="color: red;"></p>
</section>

<script type="module">
    /* const form = document.getElementById('addArtworkForm');
    const errorElem = document.getElementById('addArtworkError');

    form.addEventListener('submit', async (event) => {
        event.preventDefault(); // Prevent default form submission

        // Create FormData to handle file and text inputs
        const formData = new FormData(form);

        try {
            const response = await fetch('http://localhost:3000/api/artworks', {
                method: 'POST',
                body: formData,
                credentials: 'include', // Include cookies for session
            });

            if (response.ok) {
                alert('Artwork added successfully!');
                window.location.href = '/portfolio'; // Reload the page to update artworks
            } else {
                const data = await response.json();
                errorElem.textContent = data.error || 'Failed to add artwork.';
            }
        } catch (err) {
            errorElem.textContent = 'Unexpected error. Try again later.';
            console.error(err);
        }
    }); */
</script>